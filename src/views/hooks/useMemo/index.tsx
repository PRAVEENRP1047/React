/**
 *  useMemo(function, [dependencies])
 *    useMemo lets us cache the result of a function.
 *    the function will be run again only when the dependencies of it changes
 *    the function will be called twice to check for impurities and it's dev only behaviour
 *    this type of caching is called memoization.
 */

/** Skipping expensive recalculations */

export default function UseMemo(){
  return <></>
} 


/**
 * Reference:
 * https://react.dev/reference/react/useMemo
 */